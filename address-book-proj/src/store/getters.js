export const items = state => {
    return state.items;
}

export const selectedLocale = state => {
    return state.selectedLocale;
}

export const localeLabels = state => {
    return state.localeLabels;
}